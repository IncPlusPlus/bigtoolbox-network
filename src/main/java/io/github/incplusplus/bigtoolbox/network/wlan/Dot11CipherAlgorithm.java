package io.github.incplusplus.bigtoolbox.network.wlan;

public enum Dot11CipherAlgorithm {
  // This enum is the super-enum that keeps track of what the enum values are
  NONE,
  WEP40,
  TKIP,
  CCMP,
  WEP104,
  WPA_USE_GROUP,
  RSN_USE_GROUP,
  WEP
}
